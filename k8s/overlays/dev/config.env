# Config nao-sensivel do ambiente DEV (local/kind). DB_HOST=postgres -> service in-cluster.
SPRING_PROFILES_ACTIVE=docker
DB_HOST=postgres
DB_PORT=5432
DB_NAME=mydatabase
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_FROM=no-reply@workshop.local
NOTIFICACAO_CANAL=log
