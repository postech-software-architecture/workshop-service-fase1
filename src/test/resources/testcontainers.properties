# Testcontainers configuration
# No docker.client.strategy is set intentionally to allow auto-detection.
# This ensures compatibility with Docker Desktop (Windows/macOS) and Colima (macOS).
# Colima users must set DOCKER_HOST=unix://$HOME/.colima/default/docker.sock
testcontainers.reuse.enable=false
testcontainers.ryuk.disabled=true
