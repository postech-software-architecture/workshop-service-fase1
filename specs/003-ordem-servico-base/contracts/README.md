# Contracts

Esta fase nao expoe contratos externos.

- Nenhum endpoint HTTP sera criado.
- Nenhum caso de uso publico sera adicionado.
- Nenhum schema de integracao sera publicado.

Os contratos serao definidos em incrementos futuros quando `OrdemServico` passar a ser exposta por API, aplicacao ou persistencia dedicada.
